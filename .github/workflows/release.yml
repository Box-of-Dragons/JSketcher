name: Release

# Releases are always manual — Actions → Release → Run workflow.
# One action = version + tag + GitHub release + VPS deploy.
# All policy lives in the shared workflows (StructuredChaos docs/git-rules.md).

on:
  workflow_dispatch:

jobs:
  release:
    uses: Box-of-Dragons/StructuredChaos/.github/workflows/family-release.yml@master
    permissions:
      contents: write
    secrets: inherit

  # dev work reaches protected main only through a PR merge.
  sync:
    name: Sync release branch
    needs: release
    if: always() && github.ref_name == 'dev' && !failure() && !cancelled()
    permissions:
      contents: write
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Merge dev into main via pull request
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git fetch origin main dev
          if [ "$(git rev-list --count origin/main..origin/dev)" -eq 0 ]; then
            echo "main already contains dev."
            exit 0
          fi
          gh pr create --base main --head dev \
            --title "chore(release): merge dev into main" \
            --body "Automated release sync." || true
          gh pr merge --merge dev || { sleep 5; gh pr merge --merge dev; }

  deploy:
    needs: [release, sync]
    if: always() && !failure() && !cancelled()
    uses: Box-of-Dragons/StructuredChaos/.github/workflows/family-deploy.yml@master
    with:
      branch: main
    secrets: inherit
